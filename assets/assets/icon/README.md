# App Icon

Place your app icon files here:

- `app_icon.png` — 1024x1024px, the main app icon
- `app_icon_foreground.png` — 1024x1024px, foreground layer for Android adaptive icons

Then run:
  flutter pub run flutter_launcher_icons

This will generate all platform-specific icon sizes automatically.
